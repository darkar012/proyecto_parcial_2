<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.userapp">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserApp">
        <activity android:name=".citas"></activity>
        <activity android:name=".mensajeFinal" />
        <activity android:name=".SolicitarDatosUbicacionFecha" />
        <activity android:name=".solicitarDatosUbicacion" />
        <activity android:name=".solicitarCitaDatosPersonales" />
        <activity android:name=".MainActivity" />
        <activity android:name=".register" />
        <activity android:name=".perfil">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>